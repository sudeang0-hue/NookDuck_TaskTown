/*
using UnityEngine;

public class IslandTestInput : MonoBehaviour
{
    [SerializeField] private IslandController islandController;

    private void Update()
    {
        if (islandController == null) return;

        // 키보드 숫자 1, 2, 3 입력 감지
        if (Input.GetKeyDown(KeyCode.Alpha1))
        {
            islandController.ChangeIslandStep(0);
        }
        else if (Input.GetKeyDown(KeyCode.Alpha2))
        {
            islandController.ChangeIslandStep(1);
        }
        else if (Input.GetKeyDown(KeyCode.Alpha3))
        {
            islandController.ChangeIslandStep(2);
        }
    }
}
*/